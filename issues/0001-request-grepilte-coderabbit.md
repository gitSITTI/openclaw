Title: Request: add grepilte and coderabbit (confirm names)

Description:

The team requested adding `grepilte` and `coderabbit` to the project. I could not find these packages in `package.json`. Please confirm:

- Exact package names (spelling) and preferred placement (`dependencies` vs `devDependencies`).
- Any usage examples or tests we should add.

Acceptance criteria:

- Confirmed package names
- PR created adding packages + basic usage test
